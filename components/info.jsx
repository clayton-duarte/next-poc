import styled from 'styled-components';

const Info = styled.p`
justify-content: space-between;
display: flex;
small {
    text-align: right;
    max-width: 50%;
    height: 2em;
}
`

export default Info;